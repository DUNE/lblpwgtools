FILE( GLOB src_files *.cxx )

art_make_library( LIBRARY_NAME  UtilitiesFunc
	  	  SOURCE  	${src_files}      
		  LIBRARIES     ${ROOT_BASIC_LIB_LIST}
	  	        	${CLHEP}
                )

install_headers()
install_fhicl()
install_source()
