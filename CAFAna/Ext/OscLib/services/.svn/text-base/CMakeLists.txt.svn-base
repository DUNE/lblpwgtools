art_make( SERVICE_LIBRARIES OscLibFunc
	  		    ${ART_FRAMEWORK_CORE}
                 	    ${ART_FRAMEWORK_PRINCIPAL}
                            ${ART_FRAMEWORK_SERVICES_REGISTRY}
                            art_Persistency_Common  
                            art_Persistency_Provenance  
                            art_Utilities canvas
                            ${MF_MESSAGELOGGER}
                            ${MF_UTILITIES}
                            cetlib cetlib_except
                            ${ROOT_BASIC_LIB_LIST}
           BASENAME_ONLY
        )

install_headers()
install_source()
